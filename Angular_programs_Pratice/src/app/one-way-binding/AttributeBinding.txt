The real need of Attribute binding is when there is no DOM property at all for a given html Attribute.
In performance whose property Binding is better than attribute Binding, because when were we change the attribute it will re-initialize the DOM property and the syntax is also long for attribute binding comparing to property binding..
We can use property binding when were we want...